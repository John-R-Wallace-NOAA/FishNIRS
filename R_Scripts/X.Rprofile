

# These default packages do not load until some later point. Getting to a prompt and hence looking for user interaction makes these packages load. ????????
library(stats); library(graphics); library(grDevices); library(utils); library(datasets) 

source('NN_Model_TensorFlow_Loop.R')

quit('no',,FALSE)
